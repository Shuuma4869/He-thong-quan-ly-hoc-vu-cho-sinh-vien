spring:
  session:
    timeout: ${SESSION_TIMEOUT:30m}
    redis:
      namespace: ams:session
  config:
    import: optional:file:../.env[.properties]
  application:
    name: ams
  datasource:
    url: ${DATABASE_URL:jdbc:postgresql://localhost:5433/ams}
    username: ${DATABASE_USERNAME:ams}
    password: ${DATABASE_PASSWORD:ams_local_only}
  data:
    redis:
      host: ${REDIS_HOST:localhost}
      port: ${REDIS_PORT:6379}
      password: ${REDIS_PASSWORD:ams_redis_local_only}
      timeout: 2s
  flyway:
    enabled: true
    locations: classpath:db/migration
  jpa:
    hibernate:
      ddl-auto: validate
    open-in-view: false
    properties:
      hibernate:
        jdbc:
          time_zone: UTC

server:
  port: ${SERVER_PORT:8080}
  servlet:
    session:
      cookie:
        name: AMS_SESSION
        path: /
        http-only: true
        same-site: lax
        secure: ${SESSION_COOKIE_SECURE:false}
  error:
    include-message: never
    include-stacktrace: never
  shutdown: graceful
  tomcat:
    max-http-form-post-size: 8KB

management:
  endpoints:
    web:
      exposure:
        include: health,info
  endpoint:
    health:
      probes:
        enabled: true
      show-details: never

springdoc:
  api-docs:
    enabled: ${API_DOCS_ENABLED:true}
  swagger-ui:
    enabled: ${API_DOCS_ENABLED:true}
    path: /swagger-ui.html

ams:
  phenikaa:
    enabled: ${AMS_PHENIKAA_ENABLED:false}
    session-key: ${AMS_PHENIKAA_SESSION_KEY:}
    key-version: ${AMS_PHENIKAA_KEY_VERSION:1}
    connect-timeout: ${AMS_PHENIKAA_CONNECT_TIMEOUT:8s}
    response-timeout: ${AMS_PHENIKAA_RESPONSE_TIMEOUT:15s}
    max-response-bytes: ${AMS_PHENIKAA_MAX_RESPONSE_BYTES:2097152}
  cors:
    allowed-origins: ${CORS_ALLOWED_ORIGINS:http://localhost:3000}
  sync:
    schedule:
      classes: ${SYNC_CLASSES_INTERVAL:PT3M}
      grades: ${SYNC_GRADES_INTERVAL:PT20M}
      curriculum: ${SYNC_CURRICULUM_INTERVAL:PT24H}
